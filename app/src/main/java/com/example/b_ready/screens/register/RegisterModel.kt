package com.example.b_ready.screens.register

import com.example.b_ready.app.CustomApp

class RegisterModel(private val app: CustomApp) {

    fun saveNewUser(mobile: String, pword: String) {
        // simulated database save successful
    }
}